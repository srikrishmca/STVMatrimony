<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="STVMatrimony.Views.ItemDetailPage"
              xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms" 
             xmlns:ffTransformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations">

    <StackLayout Spacing="20" Padding="15">
        <ff:CachedImage Source="{Binding ImageURL}" HeightRequest="100" WidthRequest="100" >
            <ff:CachedImage.Transformations>
                <ffTransformations:CircleTransformation />
            </ff:CachedImage.Transformations>
        </ff:CachedImage>
        <Grid ColumnDefinitions="*,*" RowDefinitions="30*,70*">
            <Label Text="Text:" Grid.Column="0" Grid.Row="0" FontSize="Medium" />
            <Label Text="{Binding Text}" Grid.Column="1" Grid.Row="0" FontSize="Small"/>
            <Label Text="Description:" Grid.Column="0" Grid.Row="1" FontSize="Medium" />
            <Label Text="{Binding Description}"  Grid.Column="1" Grid.Row="1" FontSize="Small"/>
        </Grid>
       
       
    </StackLayout>
    
</ContentPage>