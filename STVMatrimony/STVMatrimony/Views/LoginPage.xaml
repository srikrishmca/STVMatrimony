<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="STVMatrimony.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <Grid RowDefinitions="*">
            <ScrollView VerticalScrollBarVisibility="Never">
                <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,*" ColumnDefinitions="*" Padding="20">
                   
                    <StackLayout Grid.Row="2" Grid.Column="0" Padding="10,0,10,0" VerticalOptions="Center">
                        <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
                    </StackLayout>
                </Grid>
            </ScrollView>
            <!-- Here add the code that is being explained in this block number five (5)-->
        </Grid>
       
    </ContentPage.Content>
</ContentPage>